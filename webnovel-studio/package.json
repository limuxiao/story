{
  "name": "webnovel-studio",
  "version": "1.0.0",
  "private": true,
  "description": "网文写作学习目标管理台：Vite + Vue3 + TS 前端 / NestJS + SQLite 后端",
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -C server run dev",
    "dev:web": "npm -C web run dev",
    "build": "npm -C server run build && npm -C web run build",
    "start": "npm -C server run start",
    "install:all": "npm -C server install && npm -C web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
