# webnovel-studio Docker 部署环境变量（复制为 .env 后按需修改）
# 用法示例：cp .env.example .env && docker compose up -d --build

# 前端对外访问端口（默认 5278）
WEB_PORT=5278

# 可选：若希望本机直连后端 API / Swagger 调试，放开下方并设定端口
# API_PORT=5178
